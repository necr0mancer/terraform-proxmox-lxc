# Terraform for Proxmox LXCs

run `terraform init` to initialize terraform proxmox info

then run `terraform plan -out 'tf.plan'` to see proposed build plan

next, `terraform apply tf.plan` to actually build

